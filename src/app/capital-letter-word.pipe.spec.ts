import { CapitalLetterWordPipe } from './capital-letter-word.pipe';

describe('CapitalLetterWordPipe', () => {
  it('create an instance', () => {
    const pipe = new CapitalLetterWordPipe();
    expect(pipe).toBeTruthy();
  });
});
